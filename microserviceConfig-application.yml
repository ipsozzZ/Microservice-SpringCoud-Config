
spring:
  profiles:
    active: dev  # 激活开发环境配置

server:
  port: 4001

spring:
  profiles: dev  # 开发环境
  application:
    name: microservice-config-dev  # 微服务名称

server:
  port: 4002

spring:
  profiles: prod  # 开发环境
  application:
    name: microservice-config-prod  # 微服务名称


